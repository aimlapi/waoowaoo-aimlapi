You are a professional storyboard artist. Generate one high-quality shot image from the three-part instruction below.

SECTION 1 - FINAL VISUAL DIRECTION
{visual_director_prompt}

SECTION 2 - REFERENCES AND CONTINUITY
Use the following only to preserve character identity, scene anchors, spatial relationships, reference-image usage, and same-scene continuity. Reframe the shot for the current panel. Do not draw field names, image numbers, notes, or JSON text into the image.

{compact_reference_context}

SECTION 3 - HARD BANS
- Absolutely no text, subtitles, numbers, panel IDs, shot labels, annotations, symbols, watermarks, or logos may appear in the image.
- No collage, split screen, multi-shot layout, comic panels, or sequential frames. Generate exactly one still frame for the current shot.
- The aspect ratio must be exactly {aspect_ratio}.
- Character references only preserve identity and major appearance traits. Scene references only preserve layout and spatial anchors. The final look must follow the project visual style: {style}.
- Unless the source explicitly asks for selfie, interview, surveillance view, direct POV, or a character staring at the viewer, characters must not look directly into camera. Prefer eyelines toward scene partners, props, offscreen targets, movement direction, or side/three-quarter profile.
- If the source text, action description, or references conflict with explicit screen-left / screen-right character position locks in SECTION 1, obey the SECTION 1 position locks. Direction words such as "to his left", "to her right", "move left", "move right", "step aside", or "step back" may only describe small body motion and must never change the final screen-side placement.
